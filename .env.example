# CLI tool for each agent role
# Options: claude, codex, or any CLI tool that accepts -p "prompt"
DEVELOPER_CLI=claude
REVIEWER_CLI=claude
MANAGER_CLI=claude

# Server config
SERVER_URL=http://localhost:8000
POLL_INTERVAL=5

# CLI timeout in seconds per task
CLI_TIMEOUT=300
